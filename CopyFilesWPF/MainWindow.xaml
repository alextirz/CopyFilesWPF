<Window x:Class="CopyFilesWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CopyFilesWPF"
        mc:Ignorable="d"
        Title="Copy files" Height="121" Width="358">
    <DockPanel LastChildFill="True" Name="MainPanel" Margin="8">
        <Grid DockPanel.Dock="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"></ColumnDefinition>
                <ColumnDefinition Width="6*"></ColumnDefinition>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="2*"></ColumnDefinition>
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0,0,6,0">From:</TextBlock>
            <TextBlock Grid.Row="1" Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Center" Margin="0,0,6,0">To:</TextBlock>

            <TextBox Grid.Row="0" Grid.Column="1" Margin="0,6" Name="FromTextBox" TextChanged="FromTextBox_TextChanged"></TextBox>
            <TextBox Grid.Row="1" Grid.Column="1" Margin="0,6" Name="ToTextBox" TextChanged="ToTextBox_TextChanged"></TextBox>

            <Button Grid.Row="0" Grid.Column="2" Margin="8,6" Click="FromButton_Click" Name="FromButton">Select File...</Button>
            <Button Grid.Row="1" Grid.Column="2" Margin="8,6" Name="ToButton" Click="ToButton_Click">Select Folder...</Button>
            <Button Grid.Row="0" Grid.Column="3" Grid.RowSpan="2"  Margin="8,6" Background="Green" 
                    Foreground="White" Name="CopyButton" Click="CopyButton_Click" IsEnabled="False">Copy</Button>
        </Grid>
    </DockPanel>
</Window>
